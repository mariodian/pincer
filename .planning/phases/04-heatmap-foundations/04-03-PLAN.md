---
phase: 04-heatmap-foundations
plan: "03"
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
files_modified:
  - src/mainview/app.css
requirements: []
must_haves:
  truths:
    - "Heatmap cells are clearly visible in light mode"
  artifacts:
    - path: src/mainview/app.css
      provides: CSS variable definitions
      contains: "--heatmap-1"
      min_lines: 1
  key_links: []
---

<objective>
Fix heatmap color visibility in light mode by adjusting CSS lightness values.
</objective>

<context>
@src/mainview/app.css

The :root block contains heatmap CSS variables at lines 48-53. Current lightness values are too close to the white background (0.972), making heatmap-1 through heatmap-3 nearly invisible.

Background lightness: 0.972 (line 9: --content-background: oklch(0.972 0.007 322.12))
Current heatmap values (lines 49-53):
- --heatmap-1: oklch(0.927 0.074 80)
- --heatmap-2: oklch(0.807 0.124 80)
- --heatmap-3: oklch(0.707 0.154 80)
- --heatmap-4: oklch(0.637 0.184 45)
- --heatmap-5: oklch(0.577 0.245 27.33)

The gap between background (0.972) and heatmap-1 (0.927) is only 0.045 — insufficient for visibility.
</context>

<tasks>

<task type="auto">
  <name>task 1: Reduce heatmap CSS lightness values in light mode</name>
  <files>src/mainview/app.css</files>
  <action>
In the :root block (lines 47-54), replace the heatmap color definitions with darker values that provide adequate contrast against the white background (0.972):

Change:
  --heatmap-1: oklch(0.927 0.074 80);
  --heatmap-2: oklch(0.807 0.124 80);
  --heatmap-3: oklch(0.707 0.154 80);
  --heatmap-4: oklch(0.637 0.184 45);
  --heatmap-5: oklch(0.577 0.245 27.33);

To:
  --heatmap-1: oklch(0.75 0.1 80);
  --heatmap-2: oklch(0.65 0.14 80);
  --heatmap-3: oklch(0.55 0.17 80);
  --heatmap-4: oklch(0.45 0.19 45);
  --heatmap-5: oklch(0.40 0.25 27.33);

Keep chroma and lightness progression similar (higher chroma for warmer colors), but reduce lightness by ~20% to ensure visibility against light backgrounds.
</action>
  <verify>
    <automated>grep -n "heatmap-1" src/mainview/app.css | head -2</automated>
  </verify>
  <done>Light mode heatmap colors use lightness values 0.75, 0.65, 0.55, 0.45, 0.40 — clearly distinguishable from white background</done>
</task>

</tasks>

<success_criteria>
- grep returns --heatmap-1: oklch(0.75 in light mode section
- All 5 heatmap variables have reduced lightness (first number in oklch)
- Heatmap cells visually distinguishable in light mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-heatmap-foundations/04-03-SUMMARY.md`
</output>
