---
phase: 04-heatmap-foundations
plan: "04"
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
files_modified:
  - src/mainview/lib/components/incidents/Heatmap.svelte
requirements: []
must_haves:
  truths:
    - "Heatmap tooltip uses shadcn-svelte Tooltip component"
  artifacts:
    - path: src/mainview/lib/components/incidents/Heatmap.svelte
      provides: Heatmap component with shadcn tooltip
      contains: "Tooltip.Root"
      min_lines: 1
  key_links: []
---

<objective>
Replace native HTML title attribute tooltip with shadcn-svelte Tooltip component for consistent styling.
</objective>

<context>
@src/mainview/lib/components/incidents/Heatmap.svelte
@src/mainview/lib/components/ui/tooltip/index.ts

Current implementation (line 58-60) uses native title attribute:
  title={checks.length > 0
    ? `${checks.filter((c) => c.status === "ok").length} ok, ${checks.filter((c) => c.status === "degraded").length} degraded, ${checks.filter((c) => c.status === "error").length} failed`
    : "No checks"}

Required shadcn-svelte tooltip import pattern:
  import * as Tooltip from "$lib/components/ui/tooltip/index.js"

Tooltip exports: Root, Trigger, Content, Provider, Portal (and aliases Tooltip, TooltipContent, TooltipTrigger)
</context>

<tasks>

<task type="auto">
  <name>task 1: Replace native title with shadcn-svelte Tooltip</name>
  <files>src/mainview/lib/components/incidents/Heatmap.svelte</files>
  <action>
In Heatmap.svelte:

1. Add import after existing imports (after line 3):
   import * as Tooltip from "$lib/components/ui/tooltip/index.js";

2. Replace the single div (lines 55-61) with Tooltip structure:

   <Tooltip.Root>
     <Tooltip.Trigger asChild let:builder>
       <div
         class={cn("rounded-xs transition-colors duration-100", cellSize, className)}
         style="background-color: {heatmapColor};"
         {...builder}
       ></div>
     </Tooltip.Trigger>
     <Tooltip.Content>
       {checks.length > 0
         ? `${checks.filter((c) => c.status === "ok").length} ok, ${checks.filter((c) => c.status === "degraded").length} degraded, ${checks.filter((c) => c.status === "error").length} failed`
         : "No checks"}
     </Tooltip.Content>
   </Tooltip.Root>

Note: The title attribute is removed from the div. The tooltip content is moved into Tooltip.Content. The asChild prop on Tooltip.Trigger passes builders to the underlying div for accessibility.
</action>
  <verify>
    <automated>grep -c "Tooltip.Root" src/mainview/lib/components/incidents/Heatmap.svelte</automated>
  </verify>
  <done>Heatmap uses shadcn-svelte Tooltip.Root/Trigger/Content instead of native title attribute</done>
</task>

</tasks>

<success_criteria>
- grep returns 1 (Tooltip.Root present in file)
- No title={...} attribute on the div element
- Tooltip.Content contains the status breakdown text
</success_criteria>

<output>
After completion, create `.planning/phases/04-heatmap-foundations/04-04-SUMMARY.md`
</output>
