# v0.3.2

Pincer is now installable via Homebrew on macOS.

## Highlights

- **Homebrew support** — install Pincer with `brew install mariodian/tap/pincer`
- Visual polish across the UI with improved shadow and component styling
