{
  "name": "pincerd",
  "version": "0.0.9",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "bundle": "bash ./scripts/bundle.sh",
    "db:generate": "drizzle-kit generate",
    "typecheck": "tsc --noEmit",
    "format": "bunx prettier --write '**/*.{ts,js}'"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.0.0"
  }
}
