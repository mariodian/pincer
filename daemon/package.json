{
  "name": "pincer-daemon",
  "version": "0.0.9",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "db:generate": "drizzle-kit generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.0.0"
  }
}
